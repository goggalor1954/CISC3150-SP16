template <typename T>
bool test2d(T 2dArr[][]){
	int arrSize=2dArr.length();
	for(int row=0; arrSize/2; row++){
		for(int col=0; arrSize/2; col++){
			if(2dArr[row][col]!=2dArr[arrSize-row][arrSize-col]) return false;
		}
	}
	return true;
}
